# Copy to .env and adjust. PORT is the port published on YOUR machine;
# open http://<your-host>:<PORT> in a browser.
PORT=8080
# Public site origin for absolute SEO tags (og:image, og:url, canonical),
# e.g. DOMAIN=https://example.com. Leave empty for local dev (the server
# falls back to the request Host).
DOMAIN=
